const first = 2;
const second = "2";

if(first == second){ 
    console.log('true'); // == doesn't check data type checks only the value 
}else{
    console.log('false'); //=== checks value as well as data type between two variable
}